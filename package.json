{
  "name": "protractordemosdet",
  "version": "1.0.0",
  "description": "protractor demo tests",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "pretest": "RMDIR /Q/S allure-report",
    "test": "protractor conf/conf.js",
    "posttest": "allure generate allure-results --clean -o allure-report || true"
  },
  "author": "PAVAN",
  "license": "ISC",
  "dependencies": {
    "jasmine-allure-reporter": "^1.0.2",
    "protractor": "^7.0.0"
  }
}
